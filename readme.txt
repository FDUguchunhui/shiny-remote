Now I can only run shiny server as chunhui to use the package installed by RStudio logged as Chunhui.

Need a solution to make lib path of user:shiny to the same lib path as Chunhui. Maybe not possible, then have to install in global directory. 